name: 'Workflow UI'
description: 'Administrative UI for Workflow.'
package: Workflow
type: module
core: 8.x
# version: VERSION

configure: entity.workflow_workflow.collection

dependencies:
  - workflow

#stylesheets:
#  all:
#    - workflow_admin_ui.css
